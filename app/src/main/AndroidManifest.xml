<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".TexterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Texter">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Texter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Reliable path: anything actually tagged text/* (.txt, .md, .csv, .log, source
                 files served by a provider that sets the mime type correctly, etc.). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="text/*" />
            </intent-filter>

            <!-- A handful of common code/config mime types that file managers report even though
                 they aren't under text/* (JSON, XML, shell scripts). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/json" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="application/x-sh" />
                <data android:mimeType="application/x-yaml" />
            </intent-filter>

            <!-- Broad fallback: many file managers/other apps hand off code/config files (.kt,
                 .py, .gradle, .env, dotfiles with no extension, etc.) as generic
                 application/octet-stream rather than text/*, and content:// URIs from most
                 providers don't expose a real filename in the path for extension-pattern
                 matching to key off. Registering plain */* is what actually makes Texter show up
                 in the system "Open with" chooser for those files — EditorViewModel checks for
                 binary content on open and warns instead of corrupting it, rather than trying to
                 pre-filter by an unreliable path pattern. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="*/*" />
                <data android:scheme="file" android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Backs the "Share" action: wraps a cache file holding the current editor content in a
             content:// URI so other apps receive an actual file (real filename, extension-derived
             mime type) instead of the app's private storage being exposed directly. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
